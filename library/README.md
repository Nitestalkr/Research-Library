# Library Index

This directory contains structured indexes and metadata manifests.

- `arxiv/seed-ids.txt` contains arXiv IDs collected from existing project notes.
- Future metadata exports should use JSON or JSONL so scripts can validate and update them.

